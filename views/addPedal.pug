extends layout

include mixins/_pedalForm

block content
	.inner
		h2= title
		if errors
			ul.errors(data-testid="pedals.add-form.error-message")
				each err in errors
					li= err
		+pedalForm(pedal, 'add')
